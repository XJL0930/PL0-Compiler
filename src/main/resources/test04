PROGRAM nestedTest
CONST x := 1,y := 2,z:=3;
VAR a, b, c;
BEGIN

    IF a < b THEN
        WHILE c > x DO
        BEGIN
            a := a + 1;
            IF a > c THEN
                b := b * 2;
            ;
            IF a < c THEN
                b := b / 2;
        END
END
